import XCTest
@testable import CashCore

final class CashCoreTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(CashCore().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
